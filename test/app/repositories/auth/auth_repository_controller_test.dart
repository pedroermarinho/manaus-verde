import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:manaus_verde/app/repositories/auth/auth_repository_controller.dart';
import 'package:manaus_verde/app/app_module.dart';

void main() {
  initModule(AppModule());
  // AuthRepositoryController authrepository;
  //
  setUp(() {
    //     authrepository = AppModule.to.get<AuthRepositoryController>();
  });

  group('AuthRepositoryController Test', () {
    //   test("First Test", () {
    //     expect(authrepository, isInstanceOf<AuthRepositoryController>());
    //   });

    //   test("Set Value", () {
    //     expect(authrepository.value, equals(0));
    //     authrepository.increment();
    //     expect(authrepository.value, equals(1));
    //   });
  });
}
